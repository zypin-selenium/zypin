{
  "name": "zypin",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "npx -w @zypin-selenium/linter eslint . --ext .js",
    "lint:fix": "npx -w @zypin-selenium/linter eslint . --ext .js --fix"
  }
}
